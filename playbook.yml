- name: Mac bootstrap
  hosts: localhost
  connection: local
  gather_facts: true

  vars:
    machine_type: "{{ machine_type | default('personal') }}"

  roles:
    - common
    # - dotfiles
    # - macos
    # - apps_common
    # - { role: apps_personal, when: machine_type == "personal" }
    # - { role: apps_work,     when: machine_type == "work" }
