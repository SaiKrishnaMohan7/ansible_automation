- name: Test run
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Print a hello message
      debug:
        msg: "Ansible ran successfully! Machine type is {{ machine_type }}"
